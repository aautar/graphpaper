machine:
  node:
    version: stable

dependencies:
  override:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
    - npm install

test:
  override:
    - npm test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm publish
