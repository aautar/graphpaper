machine:
  node:
    version: stable

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm publish
